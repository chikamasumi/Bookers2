<h2>User info</h2>
<div><%= attachment_image_tag @user, :image, :fill, 60, 60, format: 'jpeg' %></div>
<div>
  <p>name</p>
  <p><%= @user.name %></p>
</div>
<div>
  <p>introduction</p>
  <p><%= @user.introduction %></p>
</div>
<%= link_to "編集", edit_book_path(@book.id) %>

<h2>New book</h2>
<% if @book.errors.any? %>
  can't be blankerror
<% end %>
<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book',class:'btn btn-primary' %>
<% end %>

<h2>Books</h2>
<table>
  <thead>
    <th width="145.38" heigth="24" colspan="1"></th>
    <th width="192" heigth="24">Title</th>
    <th width="400" heigth="24">Opinion</th>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
    <%= link_to 'Show', book_path(book.id) %>
      <td><%= attachment_image_tag @user, :image, :fill, 60, 60, format: 'jpeg' %></td>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>