<h2>User info</h2>

  <p>name</p>
  <%= @user.name %>


  <p>introduction</p>
  <%= @user.introduction %>


<h2>New book</h2>
<% if @book.errors.any? %>
  can't be blankerror
<% end %>
<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

<h2>Books</h2>
<div>
  <p>Title:</p>
  <p><%=@book.title %></p>
</div>
<div>
  <p>Body:</p>
<p><%=@book.body %></p>
</div>
